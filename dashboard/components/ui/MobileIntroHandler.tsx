"use client";

/**
 * MobileIntroHandler
 * Retained for backwards compatibility across providers.
 * Root "/" is now always the landing page for all devices.
 */
export function MobileIntroHandler() {
  return null;
}
